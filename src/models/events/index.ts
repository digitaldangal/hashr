/**
 * Describes all available application events.
 *
 * @export
 * @enum {string}
 */
enum Events {
    UPDATE_PROGRESS = 'updateProgress',
    SHOW_MESSAGE = 'showMessage',
}

export default Events;
